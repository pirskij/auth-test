#pragma once

#include <macro.h>

namespace lvglpp
{
	FWD_PTR(LvObj);
	FWD_PTR(LvStyle);
	FWD_PTR(LvLabel);
	FWD_PTR(LvTextarea);
	FWD_PTR(LvBtn);
} // namespace lvglpp

namespace sol::ui
{
	FWD_PTR(UIManager);
	FWD_PTR(WindowsFactory);
	FWD_PTR(Window);
	FWD_PTR(AuthWindow);
} // namespace sol::ui
